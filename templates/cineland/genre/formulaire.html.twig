{{ form_start(ajouterGenreForm) }}
{{ form_errors(ajouterGenreForm) }}

    <div class="col-md-12">

        {% if errorMessage is not null %}
                 <div class="alert alert-danger" role="alert">
                     {{ errorMessage }}
                 </div>
        {% endif %}

            <div class=" form">
                    <div class="form-group ">
                        <label class="control-label col-lg-2">
                        {{ form_label(ajouterGenreForm.nom) }}
                        </label>
                        <div class="col-lg-10">
                            {{ form_widget(ajouterGenreForm.nom) }}
                            <p class="help-block">{{ form_help(ajouterGenreForm.nom) }}</p>
                            <p class="help-block">{{ form_errors(ajouterGenreForm.nom) }}</p>
                        </div>
                    </div>


                    <div class="form-group">
                        <div class="col-lg-offset-2 col-lg-10">
                            {{ form_row(ajouterGenreForm.submit) }}
                        </div>
                    </div>

            </div>


    </div>

{{ form_end(ajouterGenreForm) }}
