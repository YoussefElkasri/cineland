{% extends "cineland/layoutGeneral.html.twig" %}
{% block titre %}
    Les détails un film
{% endblock %}
{% block contenu %}

    <center><h4>Choisissez la méthode qui vous plait pour afficher les détails d'un film !</h4></center>
    <div class="col-md-6">
        <center><h5 ><span style="color: #00a6b2">Méthode 1</span></h5></center>
    </div>
    <div class="col-md-6">
        <center> <h5 ><span style="color: #00a6b2">Méthode 2</span></h5></center>
    </div>

    <div class="col-md-6">
        <div class="content-panel">
            <table class="table table-striped table-advance table-hover" id="dynamictable">
                <thead>
                <tr>
                    <th> #ID</th>
                    <th> TITRE</th>
                </tr>
                </thead>
                <tbody>

                {% for film in films%}
                    <tr>
                        <td >{{film.id}}</td>
                        <td> <a href="{{ path('detail_film', { 'id': film.id }) }}"><button>{{film.titre}}</button></a></td>

                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
        <div class="col-lg-6">
            <div class="form-panel">
                <form action="detailFilmV2" class="form-horizontal style-form" method="POST">
                    <div class="form-group">
                        <label class="col-sm-2 col-sm-2 control-label">Film</label>
                        <div class="col-sm-10">
                            <select class="form-control" id="id" name="id">
                                <option selected="true" disabled>Choisir un film</option>
                                {% for film in films %}
                                    <option value="{{ film.id }}" >{{ film.titre }}</option>
                                {% endfor %}
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-lg-offset-2 col-lg-10">
                            <input type="submit" class="btn btn-theme" value="Valider">
                        </div>
                    </div>
                </form>
            </div>
        </div>




{% endblock %}
