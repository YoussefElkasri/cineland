{{ form_start(monFormulaire) }}
{{ form_errors(monFormulaire) }}


    <div class="col-md-12">

        {% if errorMessage is not null %}
            <div class="alert alert-danger" role="alert">
                {{ errorMessage }}
            </div>
        {% endif %}

            <div class=" form">
                    <div class="form-group ">
                        <label class="control-label col-lg-2">
                        {{ form_label(monFormulaire.nomPrenom) }}
                        </label>
                        <div class="col-lg-10">
                            {{ form_widget(monFormulaire.nomPrenom) }}
                            <p class="help-block">{{ form_help(monFormulaire.nomPrenom) }}</p>
                            <p class="help-block">{{ form_errors(monFormulaire.nomPrenom) }}</p>
                        </div>
                    </div>
                    <div class="form-group ">
                        <label class="control-label col-lg-2">
                            {{ form_label(monFormulaire.dateNaissance) }}
                        </label>
                        <div class="col-lg-10">
                            {{ form_widget(monFormulaire.dateNaissance) }}
                            <p class="help-block">{{ form_help(monFormulaire.dateNaissance) }}</p>
                            <p class="help-block">{{ form_errors(monFormulaire.dateNaissance) }}</p>
                        </div>
                    </div>

                    <div class="form-group ">
                        <label class="control-label col-lg-2">
                            {{ form_label(monFormulaire.nationalite) }}
                        </label>
                        <div class="col-lg-10">
                            {{ form_widget(monFormulaire.nationalite) }}
                            <p class="help-block">{{ form_help(monFormulaire.nationalite) }}</p>
                            <p class="help-block">{{ form_errors(monFormulaire.nationalite) }}</p>
                        </div>
                    </div>

                    <div class="form-group ">
                        <label class="control-label col-lg-2">
                            {{ form_label(monFormulaire.film) }}
                        </label>
                        <div class="col-lg-10 col-sm-9"  >
                            <p>{{ form_widget(monFormulaire.film) }}</p>
                            <p class="help-block">{{ form_help(monFormulaire.film) }}</p>
                            <p class="help-block">{{ form_errors(monFormulaire.film) }}</p>
                        </div>
                    </div>


                    <div class="form-group">
                        <div class="col-lg-offset-2 col-lg-10">
                            {{ form_row(monFormulaire.submit) }}
                        </div>
                    </div>

            </div>


    </div>

{{ form_end(monFormulaire) }}